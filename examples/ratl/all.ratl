(define all ([Boolean] -> Boolean) (args)
    (if (not (null? args))
        (if (car args)
            (all (cdr args))
            #f)
        #t))

(define main ([Boolean] -> Boolean) (args)
    (all args))
