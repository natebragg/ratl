(define any ([Boolean] -> Boolean) (args)
    (if (not (null? args))
        (if (car args)
            #t
            (any (cdr args)))
        #f))

(define main ([Boolean] -> Boolean) (args)
    (any args))
