(define filtzero ([Nat] -> [Nat]) (xs)
    (if xs
        (if (head xs)
            (cons (head xs) (filtzero (tail xs)))
            (filtzero (tail xs)))
        []))

(define main ([Nat] -> [Nat]) (xs)
    (filtzero xs))
