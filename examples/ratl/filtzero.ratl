(define filtzero ([Nat] -> [Nat]) (xs)
    (if (not (null? xs))
        (if (not (= 0 (car xs)))
            (cons (car xs) (filtzero (cdr xs)))
            (filtzero (cdr xs)))
        []))

(define main ([Nat] -> [Nat]) (xs)
    (filtzero xs))
